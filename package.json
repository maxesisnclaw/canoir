{
  "name": "canoir",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src conformance examples test scripts",
    "official-schema": "bun run scripts/check-official-schema.ts",
    "example": "bun run examples/minimal.ts",
    "check": "bun run typecheck && bun run lint && bun test && bun run official-schema"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "0.116.0",
    "@types/bun": "^1.3.14",
    "openai": "7.4.0",
    "oxlint": "^1.77.0",
    "typescript": "^7.0.2"
  }
}
